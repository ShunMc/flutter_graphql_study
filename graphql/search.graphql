query ReadRepositories($first:Int!, $after: String) {
  viewer {
    repositories(first: $first, after: $after) {
      nodes {
        id
        name
        viewerHasStarred
      }
      pageInfo {
        endCursor
        hasNextPage
      }
    }
  }
}
